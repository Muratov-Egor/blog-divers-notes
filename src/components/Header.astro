---
import { t, localePath, alternatePath, type Locale } from "../lib/i18n";
import Search from "./Search.astro";

interface Props {
  locale: Locale;
  path: string;
}
const { locale, path } = Astro.props;

const nav = [
  { href: "/blog", label: t(locale, "nav.blog") },
  { href: "/marine-life", label: t(locale, "nav.marineLife") },
  { href: "/map", label: t(locale, "nav.map") },
];
const isActive = (href: string) => path === href || path.startsWith(href + "/");
---

<header class="sticky top-0 z-40 border-b border-line bg-ground/90 backdrop-blur">
  <div class="mx-auto flex max-w-7xl items-center gap-6 px-6 py-4">
    <a href={localePath(locale, "/")} class="flex items-center gap-2.5">
      <img
        src="/logo.png"
        alt={t(locale, "metadata.logoAlt")}
        class="h-9 w-9 rounded-full"
        width="36"
        height="36"
      />
      <span class="hidden font-display text-lg font-semibold sm:inline">Diver's Notes</span>
    </a>
    <nav class="flex flex-1 items-center gap-6 text-[15px]">
      {nav.map((item) => (
        <a
          href={localePath(locale, item.href)}
          class:list={[
            "pb-0.5 transition-colors",
            isActive(item.href)
              ? "border-b-2 border-action font-medium text-ink"
              : "text-muted hover:text-ink",
          ]}
        >
          {item.label}
        </a>
      ))}
    </nav>
    <div class="flex items-center gap-2">
      <Search locale={locale} />
      <button
        id="theme-toggle"
        aria-label={t(locale, "theme.toggle")}
        class="flex h-10 w-10 items-center justify-center rounded-full border border-line bg-surface text-muted transition-colors hover:text-lagoon"
      >
        <svg viewBox="0 0 24 24" class="h-5 w-5 dark:hidden" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z"></path>
        </svg>
        <svg viewBox="0 0 24 24" class="hidden h-5 w-5 dark:block" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <circle cx="12" cy="12" r="4"></circle>
          <path d="M12 2v2m0 16v2M4.9 4.9l1.4 1.4m11.4 11.4 1.4 1.4M2 12h2m16 0h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"></path>
        </svg>
      </button>
      <a
        href={alternatePath(locale, path)}
        class="flex h-10 w-10 items-center justify-center rounded-full border border-line bg-surface font-mono text-xs font-semibold text-muted transition-colors hover:text-lagoon"
      >
        {locale === "ru" ? "EN" : "RU"}
      </a>
    </div>
  </div>
</header>

<script>
  document.getElementById("theme-toggle")?.addEventListener("click", () => {
    const dark = document.documentElement.classList.toggle("dark");
    localStorage.setItem("theme", dark ? "dark" : "light");
  });
</script>
